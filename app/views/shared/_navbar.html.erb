<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">
    <a class="navbar-brand" href="/">Image Repository</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav ml-auto">
      <% if user_signed_in? %>
        <li class="nav-link" id="logged-in-label"> 
          Currently logged in as <span class="username-label"><%= current_user.username %></span>
        </li>
        <%= link_to "Sign out", destroy_user_session_url, method: :delete, class: "nav-link" %>
      <% else %>
        <%= link_to "Sign in", new_user_session_url, class: "nav-link" %>
        <%= link_to "Sign up", new_user_registration_url, class: "nav-link" %>
      <% end %>
      </ul>
    </div>
  </div>
</nav>